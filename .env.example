# Copy to .env and fill in values before running `envio dev`.

# Monad mainnet RPC endpoint (used for eth_calls inside handlers).
# Public endpoint: https://rpc.monad.xyz
# Or use a private node for better rate limits.
ENVIO_MONAD_RPC_URL=https://rpc.monad.xyz

# Optional: Envio API key for HyperSync access (speeds up historical sync).
# ENVIO_API_KEY=your_api_key_here
